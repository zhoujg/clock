# 2026-06-18 工作日志

## 10:55 — 粒子动画性能紧急优化

**问题**：添加 HSL 色彩+辉光+拖尾后 animation 极其慢。

**根因**：
1. `ctx.shadowBlur` 在每帧每个粒子和每条连线都执行，GPU 负担极大（~300 粒子 + ~2000 连线 × shadowBlur）
2. 粒子数 300 上限过高
3. 连线范围 130px 过大

**优化**：
- 粒子 `draw()`：移除 `shadowBlur` + `save/restore`，改用双圈绘制（大半径低透明度外圈模拟辉光，比 shadowBlur 快 10x+）
- `_drawLines()`：移除 `shadowBlur` + `save/restore`，改用平方距离预筛（`distSq > maxDist²` 跳过 sqrt）
- 粒子数量：`area/5000`(120~300) → `area/8000`(100~200)
- 连线参数：maxDist 130→100，maxAlpha 0.3→0.18，线宽缩减
- 版本号 → `20260620v`

## 09:48 — 清理死代码：移除旧版粒子动画文件

**删除的文件**：
- `scripts/particle.js` — 旧版粒子效果，已由 `plugins/particle-lines` 替代
- `scripts/animation.js` — 旧版 AnimationManager，已由 `plugins/particle-lines` 替代
- `styles/animation.css` — 旧版动画样式，已由 `plugins/particle-lines/style.css` 替代

**清理的代码**：
- `bgm-music/plugin.js` `_getAnimationAPI()`：移除旧版 AnimationManager 兼容回退
- `app.js` `initializeControls()`：移除未使用的 `animationBtn` 变量声明
- `README.md`：移除三个文件引用

## 09:53 — 动画线条按钮改为算法选择弹窗

**用户需求**：插件启动即自动开启，无需控制面板再手动启动；按钮改为弹出算法选择弹窗。

**改动**：

1. **`plugins/particle-lines/plugin.js`**：
   - `onActivate()`：末尾添加 `setTimeout(() => _start(), 100)` 自动启动
   - `_wireSettingsButton()`：点击改为弹出/关闭算法选择器，不再做 on/off 切换
   - 新增 `_createAlgoPicker()` / `_showAlgoPicker()` / `_hideAlgoPicker()`：弹窗显示 5 个算法模组，当前算法打勾高亮
   - 长按仍保留快速切换算法功能

2. **`plugins/particle-lines/style.css`**：
   - 新增 `.particle-algo-picker` 弹窗样式（毛玻璃背景、淡入动画）
   - 新增 `.particle-algo-item` 项目样式（hover 高亮、当前项打勾）

3. **`scripts/app.js`**：
   - 移除 `plugin-activated` handler 中 particle-lines 的 animationEnabled 检查逻辑
   - 移除 `restoreSettings()` 中 animationEnabled 的恢复/停止逻辑
   - 移除 `restoreSettings()` 中的 `updateAnimationStatus()` 调用
   - 移除 `saveCurrentSettings()` 中的 `animationEnabled` 字段

4. **`scripts/storage.js`**：
   - 移除 `getDefaultSettings()` 中的 `animationEnabled: false` 默认值

## 10:13 — 修复时钟位置偏移（粒子插件导致）

**根因**：删除 `animation.css` 后，`#animationCanvas` 的 `position: fixed` 样式不再在页面加载时同步生效。粒子插件通过 JS 动态注入 CSS，在注入完成前，canvas 以默认 inline 元素参与 body 的 flex 布局（display: flex; justify-content: center），将时钟推到右边并被 overflow: hidden 裁掉。

**修复**：将 `#animationCanvas` 的定位样式移回 `main.css`（同步加载），确保页面加载时立即生效。`plugins/particle-lines/style.css` 中移除重复的 canvas 样式，只保留弹窗样式。同时回退之前对 `.tick` 的错误修复（width:fit-content 等）。

## 10:43 — 粒子线条按钮改为插件动态生成

**用户需求**：删除 `index.html` 中硬编码的 `#animationBtn`，由粒子插件激活时动态向设置面板插入按钮。

**改动**：
1. **`index.html`**：删除 `.setting-item#animationBtn` 硬编码设置项
2. **`plugins/particle-lines/plugin.js`**：
   - `_createSettingsItem()`：在 `.panel-content` 中动态创建 `.setting-item#particleLinesSetting`，插入到 `#pluginLibrarySetting` 之前
   - `_removeSettingsItem()`：停用/卸载时移除动态按钮和弹窗
   - `_updateAlgoStatus()`：改用动态按钮内的 `.setting-status` 元素
   - `_showAlgoPicker()`：改用 `_settingItemEl.getBoundingClientRect()` 定位
   - `_stop()`：移除对不存在 DOM 元素的访问
   - 点击外部关闭逻辑改用 `_settingItemEl` 引用
3. **`scripts/app.js`**：删除 `updateAnimationStatus()` 方法、`plugin-activated` 中 particle-lines handler、构造函数中相关注释、`_initControls()` 中动画按钮注释

## 23:24 — 粒子对话框多选+不遮盖背景+移除状态显示

**用户需求**：
1. 设置面板点击粒子线条按钮 → 弹出对话框，支持多选算法（checkbox + 确定按钮）
2. 默认不遮盖原有背景（clearRect 替代黑色拖尾 fillRect）
3. 设置面板中不显示当前算法名称

**改动 — `plugins/particle-lines/plugin.js`**：
- 新增 `_selectedAlgos` (Set) 存储多选算法索引、`_wasMultiSelect`、`_tempSelected`
- `_updateTargets()` 重写：单选直接执行，多选对 target 取算数平均、hue 取三角函数平均
- `_animate()`：`clearRect` 替代 `fillRect('rgba(0,0,0,0.08)')` 不遮盖背景
- 单选模式保留自动轮播；多选模式由用户完全支配
- `_createAlgoPicker()` 改为完整对话框：标题栏 + checkbox 列表 + 取消/确定按钮
- `_showAlgoPicker()`：弹窗定位在按钮左侧，同步选中状态到 checkbox
- `_applySelection()`：从 checkbox 读取多选，至少保留一个
- `_createSettingsItem()`：移除 `setting-status` span
- `_cycleAlgorithm()`：切换到新算法时重置为单选模式
- 删除无调用者的 `_updateAlgoStatus()`
- 删除重复的 `_removeCSS()` 定义

**改动 — `plugins/particle-lines/style.css`**：
- 完全重写：对话框毛玻璃样式、自定义 checkbox（选中 → 蓝底白勾）、取消/确定按钮、入场缩放动画
- 版本号 → `20260618g`

## 11:11 — 粒子对话框改为单选 radio + 自动轮播

**用户反馈**：多选 checkbox "选了一个好像不起作用"，改为只允许单选或全选循环。

**根因**：`_algos` 通过 `_initAlgorithms()` 在 `_start()` 中延迟初始化，若弹窗先创建则列表为空；多选混合逻辑过于复杂且不可预期。

**改动 — `plugins/particle-lines/plugin.js`**：
- `_algos` 提升到模块顶层立即初始化（name/icon 有效，fn 通过 `_bindAlgoFns()` 绑定），确保弹窗创建时总有数据
- 移除多选体系（`_selectedAlgos`/`_wasMultiSelect`/`_tempSelected`），回归简单单选
- 新增 `_autoCycle` 布尔变量，控制自动轮播模式
- `_animate()`：仅在 `_autoCycle===true` 时自动切换（每 8 秒）
- `_updateTargets()`：恢复为直接调用 `_algos[_algoIndex].fn`
- 对话框改为 radio button（`name="algoSelect"`，点击即生效，无需确定按钮）
- 底部新增分隔线 + 🔄 自动轮播选项（value="-1"）
- `_showAlgoPicker()`：同步 radio 选中 + 绑定 onchange 立即生效
- 删除 `_initAlgorithms()`/`_updateAlgoStatus()`/`_applySelection()` 等冗余函数
- `transitionDuration` 从 2000ms → 8000ms（自动轮播间隔更合理）

**改动 — `plugins/particle-lines/style.css`**：
- 自定义 radio 样式（圆形 + 蓝点指示器）
- 移除底部确认/取消按钮相关样式
- 新增分隔线和自动轮播行样式

## 11:17 — 创建 Halftime·此间半刻 插件

**用户需求**：极简点阵视觉语言插件，展示时间流逝。包含：
- 时间点阵视图：今日24小时（环形时钟）、本周7天、本月~30天、今年365天、一生4000周
- 生之钟 / Life Progress：设置生日+预期寿命，以圆环进度呈现人生进度

**新建文件**：
- `plugins/halftime/plugin.js`（654行）：IIFE 闭包，6个视图 tab（今日/本周/本月/今年/一生/生之钟），环形时钟（24点围圆+中心时间）、周横排、月网格、年密集网格、一生50列点阵。生之钟含 date 输入+range slider 设置，圆环 SVG+渐变色进度条、3个统计数字、6段人生文案。
- `plugins/halftime/style.css`（455行）：毛玻璃弹窗、点阵5种尺寸（tiny/small/medium/large/xlarge）、pulse动画、SVG环形进度、响应式

**修改文件**：
- `plugins/registry.json`：添加 halftime 条目
- `scripts/pluginLibraryUI.js`：`_REGISTRY.plugins` 中添加 halftime
- `scripts/pluginManager.js`：`init()` 中添加 halftime 自动安装为核心插件
- `index.html`：main.css 版本 → `20260618h`

## 11:30 — 修复 Halftime 设置按钮不显示

**根因**：`_createSettingItem()` 中用 `document.querySelector('.settings-list')` 查找容器，但实际容器是 `.settings-panel .panel-content`（参照 particle-lines 插件），导致 `if (!settingsList) return;` 提前退出。

**修复**：改为 `document.querySelector('.settings-panel .panel-content')`，变量名 `settingsList` → `panelContent`。

## 11:36 — 粒子线条增加透明模式复选框

**需求**：设置弹窗中增加「透明模式」复选框，默认不勾选（不透明=有拖尾残影）。

**改动 — `plugins/particle-lines/plugin.js`**：
- 新增 `_transparent = false` 状态变量
- `_animate()`：`_transparent` 为 true → `clearRect`（透明）；为 false → `fillRect('rgba(0,0,0,0.08)')`（拖尾）
- `_createAlgoPicker()`：自动轮播下方新增分隔线 + 复选框行（👁️ 透明模式 · 不遮盖背景）
- `_showAlgoPicker()`：同步复选框状态、绑定 onchange 事件，pickerH 260→310

**改动 — `plugins/particle-lines/style.css`**：
- 新增 `.algo-checkbox-item` 和 `.algo-checkbox` 样式（仿 radio 风格，选中蓝色背景+白色勾）

---

### 粒子插件竖屏适配 + 默认自动轮播

**问题**：竖屏手机上点击「粒子线条」弹出对话框跑到屏幕外面看不到；用户期望默认自动轮播。

**改动 — `plugins/particle-lines/plugin.js`**：
1. `_showAlgoPicker()` 定位逻辑重写：四向自适应（左>右>水平居中，上>下>垂直居中），确保窄屏时对话框始终可见且不超出视口
2. `_start()` 中 `_autoCycle = false` → `_autoCycle = true`，首次激活默认自动轮播

---

### 粒子线条设置面板位置调整

`_createSettingsItem()` 中 `insertBefore(item, pluginLibItem)` → `insertBefore(item, pluginLibItem.nextSibling)`，粒子线条按钮放在插件库按钮下方。

---

### 新增：创意万年历插件

**文件**：`plugins/creative-calendar/plugin.js`（~430行）+ `plugins/creative-calendar/style.css`（~240行）

**功能**：
- 自包含农历计算引擎（1900-2100年数据表，hex编码：低4位=闰月，位4-15=大小月，位16=闰月大小）
- 节气计算（基于太阳黄经/儒略日天文近似算法，±1天精度）
- 月视图：7×6网格，公历日期+农历日期+节气+节日
- 公历节日（元旦、国庆等）+ 农历节日（春节、中秋、端午等）自动标注
- 干支纪年 + 生肖显示
- 当月标记：金色边框+底部圆点指示；周末淡红；非当月灰暗
- 中国风暗色美学：暖金调文字、水墨渐变背景、毛玻璃阴影
- 响应式：420px宽弹窗，超窄屏自适应缩小

**集成**：
- 设置面板动态生成「📅 万年历」按钮
- 已注册到 `registry.json` 和 `pluginLibraryUI.js`

### 万年历修复：节气数字 bug（18:05）
- `_calcSolarTerms()` 中 JD 带小数导致 `_jdToDate` 转换后日/月变成浮点数
- **修复**：`const jd = Math.round(baseJD + _solarTermOffset(year, angle))` — 对 JD 取整后再转日期

### 长按日期弹出万年历（11:50）
- **`plugins/creative-calendar/plugin.js`**：IIFE 末尾暴露 `window.__creativeCalendar = { show, hide, isOpen }`
- **`scripts/app.js`**：新增 `_initDateLongPress()` 方法，在 `initializeDateDisplay()` 尾调用
  - `#dateText` 元素上绑定 pointerdown/pointermove/pointerup/pointercancel/pointerleave
  - 长按阈值 500ms，移动超过 10px 取消
  - 检查 `PluginManager.isInstalled('creative-calendar') && isEnabled('creative-calendar')`
  - 有触觉反馈 `navigator.vibrate(15)`
  - 阻止 `contextmenu` 防止系统菜单
  - 设置 `userSelect: none` + `cursor: pointer`
- `index.html`：app.js 版本 → `20260618a`

### 万年历颜色对比度增强（11:57）
用户反馈看不清，全面增强 `plugins/creative-calendar/style.css` 所有文字颜色：
- **公历日期**：`0.85` → `0.95` 不透明度 + `text-shadow` 微阴影
- **农历日期**：`0.45` → `0.62` 不透明度，颜色增亮
- **节气文字**：`0.65` → `0.82` 不透明度，绿色更鲜明 `rgba(120,220,150)`
- **周末日期**：`0.5` → `0.7`
- **非当月日期**：`0.12` → `0.22`（至少可见）
- **星期标题**：`0.4` → `0.55`
- **今日高亮**：背景增亮 + 边框加粗到 2px
- **节日高亮**：颜色增亮 + 发光 text-shadow
- **节气底部行**：`0.55` → `0.68`
- **节日底部行**：`0.5` → `0.62`
- **导航按钮/关闭按钮**：不透明度提升
- **干支年份**：`0.55` → `0.68`
- **弹窗背景**：从 `22,22,36/16,16,28` 微调到 `28,28,44/20,20,36`

### 万年历浅色系主题（12:01）
暗色主题仍看不清，改为完整浅色温润主题 `style.css`：
- **弹窗背景**：乳白渐变 `#fefcf7 → #f8f3ea`
- **公历日期**：`#3a3022` 深暖灰，加粗 600
- **农历日期**：`#9a8b78` 中暖灰
- **节气文字**：`#388040` 森林绿，加粗 600
- **周末**：`#c05545` 暗红
- **节日(公历)**：`#e88730` 暖橙
- **节日(农历)**：`#d04030` 朱红
- **今日高亮**：`#fdf3d0` 淡金背景 + `#e8c45a` 金框
- **非当月**：`#c8beb0` / `#d5cdc2` 浅灰
- **星期标题**：`#b0a090`，周末 `#c08070`
- **底部文字**：节气 `#5a8050`，节日 `#c07040`
- **导航/关闭**：`#a89980` → hover `#5a4a32`
- **阴影**：轻量化 `rgba(0,0,0,0.12)`
- CSS 通过 `Date.now()` 自动刷新

### 万年历与每日故事关联 — 方案C（12:05）
**creative-calendar/plugin.js**：
- `_render()` 中读取 `localStorage.dailyStories` 获取当月故事数据（松耦合，检查 `isInstalled('daily-stories')`）
- 日期格子里有故事时显示标记点：蓝色小圆标显示故事数量，全部完成变绿色实心圆
- 点击有故事的日期 → 关闭日历 → 导航到该日的每日故事面板（`dailyStoriesManager.navigateToDate` + `togglePanel`）
- `_createModal()` 中监听 `stories-updated` 事件，日历打开时自动刷新标记

**dailyStories/dailyStories-class.js**：
- `saveTodayStories()` 中 `localStorage.setItem` 后 `dispatchEvent(new CustomEvent('stories-updated'))`

**style.css**：
- `.cc-story-dot` — 蓝色圆标（未完成故事数）+ `.cc-story-done` 绿色实心圆（全部完成）

---

### 音乐播放器插件 Apple 浅色主题重写（12:41）⚠️ 修正

上次（12:35）误改了旧文件 `styles/bgmPlayer.css`，实际音乐播放器是 `plugins/bgm-music/` 插件。
**正确重写 `plugins/bgm-music/style.css`**（全文件）：
- 弹窗背景：`rgba(0,0,0,0.85)` → `rgba(0,0,0,0.4)` + blur
- 面板内容：暗蓝渐变 `#1a1a2e→#16213e` → 白色毛玻璃 `rgba(255,255,255,0.92)` + `blur(40px) saturate(180%)`
- 所有文字：`white/rgba(255,...)` → `#1d1d1f / #86868b`
- 紫色系强调色 `#667eea/#764ba2/#8a2be2` → Apple 蓝 `#0071e3`
- 唱片动画：紫色光晕 → 蓝色光晕
- 进度条/音量条：紫色 → 蓝色
- 循环激活：紫色 → 蓝色 `#0071e3`
- 收藏激活保持 `#ff3b30` 红色
- `plugin.js` CSS版本号 → `v=20260618a` 强制缓存刷新

### 每日故事面板统一浅色主题（12:36）

`plugins/daily-stories/dailyStories-class.js` — `applyPanelBackground()`：
- 移除自动检测背景亮度切换深/浅模式的逻辑
- 统一使用 Apple 浅色主题：`#f5f5f7 → #fafafa` 渐变 + `#1d1d1f` 文字色
- 始终移除 `dark-bg` 和 `stories-dark` 类
- 语法检查通过

### 励志语样式恢复（12:35）

`styles/main.css` 励志语区域恢复为深色半透明风格：
- 背景 `rgba(0,0,0,0.25)` + `blur(20px)` + 白色边框
- 英文 `rgba(255,255,255,0.95)` + 深色阴影
- 中文 `rgba(255,255,255,0.75)` + 深色阴影

### 默认背景改为湿沥青（12:35）

`scripts/storage.js` 默认 backgroundColor：`#f5f5f7` → `#34495e`（湿沥青）

---

### 故事页面头部按钮布局重构（12:44）

用户反馈按钮布局不协调，重构 `.stories-panel-header` 从 side-by-side 改为两行结构：

**CSS 改动** (`plugins/daily-stories/style.css`)：
- `.stories-panel-header`：`flex+space-between+align-center` → `flex-wrap+flex-start`（左列不再垂直居中右侧按钮）
- `.stories-header-left`：`width: 100%` + `padding-right: 60px`，垂直排列标题/日期/焦点/Tab
- `.stories-header-right`：`position: absolute; top: 20px; right: 24px`，3个图标按钮固定右上角
- 图标按钮统一样式：36×36 圆角方块 + 统一 hover 效果（蓝/橙/红 各色）
- `.stories-header-title`：绿色药丸 → Apple 蓝 `rgba(0,113,227,0.06)` + `#1d1d1f` 文字
- `.stories-count-circle`：绿色背景 → `#0071e3` 蓝色
- `.stories-date-btn` / `.stories-date-label` / `.stories-date-today`：紫色系 → Apple 蓝
- `.stories-view-tabs`：增大 padding + 外边框 + 更大圆角
- 移除重复的按钮样式块（旧版 ~80行冗余代码）
- CSS brace 平衡校验通过

### 故事状态筛选按钮重构（12:51）
用户要求移除「今天」tab，将「进行中」「待开始」放到日期导航按钮后面。

**JS 改动** (`plugins/daily-stories/dailyStories-class.js`)：
- 移除 `stories-view-tabs` 容器中的三个 tab 按钮，替换为隐藏容器（仅保留兼容）
- 在 `.stories-date-nav` 内添加 `.stories-status-sep` 分隔线 + 三个 `.stories-status-btn`（今天/进行中/待开始）
- `switchView()`：选择器 `.stories-view-tab` → `.stories-status-btn`，非 today 视图用 `in-global-view` class 代替 `display:none`
- 事件委托从 `#storiesViewTabs` → `.stories-date-nav`

**CSS 改动** (`plugins/daily-stories/style.css`)：
- 新增 `.stories-status-sep`（竖线分隔） + `.stories-status-btn`（胶囊按钮）样式
- `.stories-status-btn.active`：`#0071e3` 蓝色高亮 + 淡蓝背景
- `.stories-date-nav.in-global-view .stories-date-btn/.stories-date-label/.stories-status-sep`：隐藏日期导航控件，保留状态按钮
- 旧 `.stories-view-tabs` / `.stories-view-tab` 改为 `display:none`
- 移动端媒体查询改为 `.stories-status-btn`

### 故事页面头部精简重构（13:09）
用户要求：去掉标题行、删掉两个「今天」（📅今天按钮+今天状态按钮）、统计/回顾按钮移到日期导航行内联（待开始后面）。

**JS 改动** (`dailyStories-class.js`)：
- 移除 `.stories-header-title` 标题药丸整块 HTML
- 移除 `#storiesDateToday` 按钮（回到今天的「今天」）
- 移除 `data-view="today"` 的状态筛选按钮
- 状态按钮仅保留 `[进行中](inProgress)` 和 `[待开始](pending)`
- 统计/回顾按钮从 `.stories-header-right` 绝对定位区移入 `.stories-date-nav` 行内流，用 `.stories-header-sep` 竖线分隔
- 关闭按钮独立为绝对定位圆形按钮（右上角）
- 删除 `.stories-header-right` 容器和 `.stories-view-tabs`

**CSS 改动** (`style.css`)：
- `.stories-header-left` padding-right 60→44px（仅关闭按钮占位）
- `.stories-close-btn` → `position:absolute; border-radius:50%`
- `.stats-btn/.review-btn` 从 36×36 方块改为 32×30 行内按钮
- 新增 `.stories-header-sep` 竖线分隔符
- 所有旧 title/header-right 响应式规则 → `display:none`
- CSS brace 平衡修复（替换残留孤立属性）

### 进行中/待开始视图保留日期导航（13:35）
`.in-global-view` 不再隐藏 `.stories-date-btn` 和 `.stories-date-label`，仅隐藏 `.stories-status-sep`。日期选择按钮在任意视图下始终可见。

### 修复：全局视图下点击日期导航无变化（13:38）
**问题**：在「进行中/待开始」视图点击日期前后切换，`navigateToDate` 更新了 `viewingDate` 和 stories，但 `currentView` 仍保持 `'inProgress'/'pending'`，`updateUI()` 走 `updateUIForGlobalView()` 渲染全局视图，忽略了日期变更。

**修复**：`navigateToDate()` 中，若 `currentView !== 'today'`，自动切回 today 视图（重置 `currentView`、更新状态按钮 active、移除 `in-global-view` class、恢复 focusDisplay）。

### 修复：全局视图卡片之间缺少间距（13:48）
**问题**：进行中/待开始视图下，`.stories-global-date-group` 仅 `margin-bottom:16px`，组内卡片无间距，紧贴在一起。

**修复**：`.stories-global-date-group` 添加 `display:flex; flex-direction:column; gap:14px`，组内卡片获得 14px 间距。不影响今日视图（使用 grid gap）。

### 全局视图支持编辑和番茄钟（13:50）
**问题 1**：进行中/待开始视图点击日期导航无变化（已在 13:38 修复）。

**问题 2**：全局视图中故事卡片不支持番茄钟和编辑。

**根因**：
1. 番茄钟按钮仅在 `currentView === 'today'` 时渲染
2. `createDimensionCard` 中 `this.stories.indexOf(story)` 因 JSON.parse 引用不同始终返回 -1，导致 data-index="-1"
3. 全局视图点击故事走 `showStoryDetailGlobal` 只读详情，不走编辑弹窗

**修复**：
- `getAllStoriesGlobal`：携带原始索引 `{ story, dateStr, index: idx }`
- `updateUIForGlobalView`：设置 `story._globalIndex` 和 `story._globalDate`
- `createDimensionCard`：优先使用 `story._globalIndex` 作为 index
- `createTaskItem`：番茄钟按钮在全局视图也渲染，添加 `data-source-date` 属性
- 点击事件：pomodoro 按钮全局视图 → `linkToPomodoroGlobal(sourceDate, index)`；taskItem 全局视图 → `showEditStoryGlobal(sourceDate, index)` 打开编辑弹窗
- 新增 `showEditStoryGlobal`：设置 `_editSourceDate`，临时替换 `this.stories`，调用 `showAddStoryModal`
- `showAddStoryModal`：`isGlobalEdit` 标记绕过过往日期只读限制
- `saveStoryFromModal`：`_editSourceDate` 存在时保存到原始日期 localStorage，恢复 stories 后刷新全局视图
- `closeAddStoryModal`：清除 `_editSourceDate`
- `deleteStory`：`_editSourceDate` 存在时保存到原始日期

### 修复：全局视图番茄钟报错 startPomodoro is not a function（13:58）
**问题**：`linkToPomodoroGlobal` 调用 `this.pomodoroTimer.startPomodoro()`，但 PomodoroTimer 类无此方法。

**修复**：按 `linkToPomodoro` 的模式重写，使用实际 API：`setMode('work')` → `timeRemaining = 25*60` → `updateDisplay()` → `start()`。同时添加标题校验、pomodoroTimer 空检查、运行中提示、关闭面板和开始通知。

### 日期标签点击打开日期选择器（14:08）
**需求**：进行中/待开始视图下，点击日期标签应能选择任意日期跳转。

**实现**：
- `storiesDateLabel` 添加 click 事件 → `navigateToDate(this.viewingDate)`，直接切换到当前显示日期的故事
- 利用已有的 `navigateToDate` 中 global→today 切换逻辑
- CSS 保留 `cursor: pointer` 和 hover 高亮

### 颠覆性番茄钟重设计：聚焦之环（14:14-14:19）
**设计理念**：彻底消除传统下拉面板，番茄钟变为沉浸式环境体验。

**核心改动**：
1. **无面板交互**：单击🍅 = 开始/暂停 25min 工作；长按🍅 = 径向模式菜单
2. **聚焦光环**：Canvas 渲染的发光环围绕时钟，进度弧从12点顺时针增长，多层光晕 + 端点发光
3. **环境粒子**：25 个发光粒子环绕时钟，工作模式金黄/快速，休息模式青/缓慢，完成时爆发 40 个粒子
4. **背景呼吸**：径向渐变叠加层，工作模式暖琥珀色，休息模式冷青色
5. **径向菜单**：4 个按钮在🍅上方弧形排列，带动画延迟入场
6. **自定义弹窗**：毛玻璃对话框，仿 Apple 风格
7. **会话点**：按钮上方小圆点显示已完成 session，最近 4 个高亮
8. **完成庆祝**：光环闪光 + 粒子爆发 + 三音符上行和弦音效
9. **自动过渡**：工作完成 2s 后自动切换休息模式并开始，休息完成切换回工作

**文件变更**：
- `scripts/pomodoro.js`：完全重写（454→520 行），移除所有面板逻辑
- `styles/pomodoro.css`：完全重写（509→370 行），新增加 Canvas 层、呼吸层、径向菜单样式
- `index.html`：移除旧番茄钟面板（60 行 HTML）
- 保持所有系统集成（forest/achievement/stories/clockManager）API 不变

### 修复：短休/长休不显示（14:48）
**问题**：径向菜单 4 个按钮只有 2 个可见。原因：弧心用 `btnRect.top`，角度用上方弧 (-PI→0)，按钮在右上角所以上方 2 个超出视口。

**修复**：弧心改为 `btnRect.bottom + 8`，角度改为下方弧 (3PI/4→PI/4)，所有四项位于按钮下方可见区域。

### 长按直接弹出自定义时长 + 记忆上次值（14:52）
**需求**：长按🍅直接弹出时长输入框；输入的时长需记住，下次预填。

**实现**：
- `bindEvents()`：长按直接调 `promptCustomDuration()` 替代 `showRadialMenu()`
- `promptCustomDuration()`：从 `localStorage.pomodoroCustomDuration` 读取上次值，默认 25
- `startCustomTimer()`：写入 `localStorage.pomodoroCustomDuration`，更新 `radialCustomTime` 标签
- `createRadialMenu()`：自定义按钮 `radialCustomTime` 同步显示已保存的时长

### 删除森林功能（14:55）
**需求**：删除整个虚拟森林系统。

**删除内容**：
- `scripts/forest.js`（679行）和 `styles/forest.css`（972行）物理删除
- `index.html`：移除 CSS link、`growing-tree-display` HTML、script 标签
- `scripts/app.js`：移除 `new ForestSystem()`、PomodoroTimer 构造参数的 `forestSystem`、`setSystemReferences` 中的 forest 参数、`updateUI` 中的 forest 刷新
- `scripts/pomodoro.js`：移除构造函数 `forestSystem` 参数、`this.forestSystem` 属性、所有 `startPlanting/updateTreeProgress/abandonPlanting/completePlanting` 调用
- `scripts/syncAdapter.js`：移除 keyMap 中的 `forestData`
- `plugins/daily-stories/dailyStories-class.js`：移除 `this.forestSystem`、`setSystemReferences` forest 参数、`plantSpecialTree` 方法
- `server/api/register.php`、`server/api/sync.php`：移除 `forestData` 键
- `server/sql/schema.sql`：移除注释中的 `forestData`
- `README.md`、`DEPLOYMENT.md`、`doc/同步逻辑说明.md`：移除森林相关章节和记录

### 删除荣誉/成就系统（15:04）
物理删除 `scripts/achievement.js`（~907行）、`styles/achievement.css`（~690行）、`styles/achievement-page.css`（~620行），总计约 2200 行代码。

清理 10 个源文件：
- `index.html`：移除 achievement.css link、🏆 控制按钮+徽章 HTML、achievement.js script 标签
- `scripts/app.js`：移除 `new AchievementSystem()`、PomodoroTimer 构造参数、setSystemReferences 的 achievement 参数
- `scripts/pomodoro.js`：构造器移除 achievementSystem 参数和属性，移除 `onPomodoroComplete()` 调用
- `plugins/daily-stories/dailyStories-class.js`：移除 `this.achievementSystem` 属性、`setSystemReferences` 的 achievement 参数、`checkStoryAchievements()` 方法（~38行）、两处 `addExp()` 调用
- `scripts/syncAdapter.js`：移除 keyMap 中的 `achievements` → `studyAchievements` 映射
- `server/api/register.php`、`server/api/sync.php`：移除 validKeys 中的 `achievements`
- `server/sql/schema.sql`：移除注释中的 `achievements`
- `README.md`、`DEPLOYMENT.md`、`doc/同步逻辑说明.md`：移除成就相关记录

### 故事面板底部固定工具栏（15:15）
用户反馈顶部按钮挤在一起，将日期导航+状态筛选+统计/回顾按钮移到页面底部固定栏。

**改动 — `plugins/daily-stories/dailyStories-class.js`**：
- `createUI()`：panel header 精简为仅 close 按钮 + focus display
- 新增 `.stories-bottom-bar > .stories-bottom-nav`，包含 date prev/next/label + 状态按钮（进行中/待开始）+ stats + review
- `.stories-date-nav` 引用全部改为 `.stories-bottom-nav`（navigateToDate、bindEvents、switchView、togglePanel）
- `switchView` 中 `in-global-view` class 切换到 bottomNav

**改动 — `plugins/daily-stories/style.css`**：
- `.stories-panel-header`：flex+space-between，简洁 header
- `.stories-close-btn`：取消 absolute 定位，改为 flex 子项
- `.stories-header-left`：删除整个规则块
- `.stories-stats-btn/.stories-review-btn`：取消 inline-block 属性
- `.stories-header-sep/.stories-status-sep`：取消 inline/dispaly 冗余属性
- `.stories-status-btn`：inline-flex → flex
- 新增 `.stories-bottom-bar`：fixed bottom，毛玻璃 blur(20px) saturate(180%)，safe-area 适配
- 新增 `.stories-bottom-nav`：居中的胶囊形容器，bg+圆角
- `.stories-list` 底部 padding 40→100px 避免被底部栏遮挡
- 深色模式新增 `.stories-panel.dark-bg .stories-bottom-bar/.stories-bottom-nav` 样式
- 移动端适配：header/list/bottom-bar 三处 padding 调整

## 15:21 — 统计/回顾按钮移回顶部 header

用户反馈：待开始右侧两个按钮（统计、回顾）应放回原先位置（顶部 header）。

**改动**：
- `dailyStories-class.js`：stats/review 按钮从 `.stories-bottom-nav` 移至 `.stories-header-actions`（与 close 按钮同组）
- `style.css`：新增 `.stories-header-actions`（flex + gap: 6px），header 布局不变
- 底部栏精简为：◀ 日期标签 ▶ | 进行中 | 待开始

## 15:23 — 首页按钮重排：番茄钟/音乐移至底部工具栏

用户要求：首页上方只保留设置和人头按钮，番茄钟、音乐按钮同故事一起放在最下面，按钮形状统一样式。

**改动 — index.html**：
- 移除 `#pomodoroToggle` 从 `.controls`
- 新增 `.bottom-toolbar` 容器（fixed bottom center，毛玻璃胶囊条），内含 `#pomodoroToggle`

**改动 — styles/controls.css**：
- 删除 `#musicBtn.active`
- 新增 `.bottom-toolbar`：`position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%);` + 毛玻璃 + 圆角胶囊
- 新增 `.bottom-tool-btn`：56x56 圆角方形，flex column，icon+label 布局
- 新增 `.tool-btn-icon` / `.tool-btn-label` 子元素样式
- 新增响应式：≤480px 缩至 48x48，横屏 ≤500px 缩至 40x40

**改动 — plugins/daily-stories/style.css**：
- `.stories-main-btn`：移除 `position: fixed; bottom/right/z-index`，改为纯 flex 子项

**改动 — plugins/daily-stories/dailyStories-class.js**：
- `createUI()`：story 按钮 append 到 `.bottom-toolbar`（fallback → `document.body`）

**改动 — plugins/bgm-music/plugin.js**：
- `MUSIC_BTN_HTML`：class 从 `control-btn` → `bottom-tool-btn`，结构改为 icon+label
- `_createUI()`：插入目标从 `.controls` → `.bottom-toolbar`，排在番茄钟之后

**改动 — styles/pomodoro.css**：
- `#pomodoroToggle`：移除 `position: relative`（由 `.bottom-tool-btn` 提供）
- `#pomodoroToggle.radial-open`：`transform: scale(1.1) !important`（覆盖 hover）

**改动 — scripts/pomodoro.js**：
- `createUI()`：session dots 从 `parentNode.insertBefore` 改为 `this.toggle.appendChild`

**改动 — scripts/app.js**：
- click-outside 关闭面板逻辑：新增 `.bottom-toolbar` 排除项

**最终布局**：
- 顶部：`[👤 认证] [⚙️ 设置]`
- 底部：`[🍅 番茄] [🎵 音乐] [📖 故事]` — 三个 56x56 统一圆角方形按钮

## 15:31 — 设置面板插件入口移至底部工具栏

用户规则：插件触发按钮统一放页面下方，有配置的（如粒子动画）才留在设置面板。

**改动 — plugins/halftime/plugin.js**：
- `_createSettingItem()` → `_createToolButton()`：从注入 `.settings-panel` 改为 append 到 `.bottom-toolbar`
- `_removeSettingItem()` → `_removeToolButton()`
- 按钮 HTML：`<button class="bottom-tool-btn">` + SVG 时钟图标 + "半刻" 标签
- `onActivate/onDeactivate/onUninstall` 回调同步更新

**改动 — plugins/creative-calendar/plugin.js**：
- 同上：`_createSettingItem()` → `_createToolButton()`
- 按钮 HTML：`<button class="bottom-tool-btn" id="ccToolBtn">` + SVG 日历图标 + "日历" 标签

**设置面板保留项**（均为有配置的）：
- 背景设置、滴答声音（开关配置）、插件库（管理入口）
- 粒子线条（动画模式选择等配置）

## 15:41 — 底部工具栏："无边框无底色，贴底摆放"

**改动 — controls.css**：
- `.bottom-toolbar`：移除 `background`/`backdrop-filter`/`border`/`border-radius`/`box-shadow`；`bottom` 改为 0 + `env(safe-area-inset-bottom)` 安全区适配
- `.bottom-tool-btn`：移除 `border`/`background`/`backdrop-filter`/`box-shadow`；默认色 `#86868b`；hover 仅 scale + 变色 `#1d1d1f`；active 缩小 0.95
- `.tool-btn-label`：`color: currentColor` 继承按钮色
- 响应式断点同步：`≤480px` / landscape 均 `bottom: 0` + safe-area

**改动 — plugins/daily-stories/style.css**：
- `.stories-main-btn`：同上风格统一（透明无边框，hover scale）
- `.stories-main-icon` / `.stories-main-text`：`color: currentColor`

## 15:41 — 激励语上移避免与工具栏重叠

- `main.css`：`.quote-container` 默认 `bottom: 12%` → `16%`；小屏横屏 `5%`→`8%`；超小横屏 `3%`→`6%`

## 15:56 — 长谚语与时钟重叠修复 + 横屏布局优化

**问题**：长英文谚语向上扩展与时钟卡片重叠。`bottom` 定位导致文字从底部向上增长。

**修复（默认）**：
- `bottom: 16%` → `22%`（更多底部空间，远离底部工具栏）
- `max-width: 85%` → `92%`（更宽区域 → 英文少换行 → 更矮）
- 新增 `max-height: 30vh` + `overflow-y: auto`（极端情况安全阀）
- 滚动条隐藏

**横屏优化（手机+平板）**：
- 手机横屏 `(max-width:900px, max-height:500px)`：日期移至左上角（`top:8px, left:12px`），时钟缩小 `20vh→14vh`，谚语 `bottom:12%, max-width:90%`
- 超小横屏 `(max-width:700px, max-height:400px)`：日期 `top:5px, left:10px`，时钟 `11vh`，谚语 `bottom:10%, max-width:90%`
- **新增平板横屏** `(min-width:601px, orientation:landscape, max-height:1200px)`：日期/天气左上角（`top:12px, left:16px`），时钟 `12vh` 上移，谚语 `bottom:18%, max-width:92%`

## 16:02 — 时钟增大 + 天气区极简化

**时钟卡片**：
- 默认 `.tick`：`15vw` → `max(18vw, 14vh)`（利用 max() 确保竖屏平板也足够大）
- 手机横屏：`14vh` → `18vh`
- 超小横屏：`11vh` → `16vh`
- 平板横屏：`12vh` → `16vh`

**天气区极简化**（去掉装饰边框/底色/图标）：
- `weather.css`：`.weather-display` 移除 `background`/`border`/`border-radius`/`backdrop-filter`/`box-shadow`/`padding`/`margin`/`cursor`/hover，改为 `inline-flex`；`.weather-icon` 设为 `display: none`；`.weather-info` 改为 `flex-direction: row`（温度+描述横向排列）
- `weather.js`：HTML 中移除 `<div class="weather-icon">` 
- `smartColor.js`：`adjustWeatherColors` 移除容器 background/borderColor 动态设置和 icon filter 设置
- 天气现为：`年月日 · 星期几  26° 晴` 一行内显示，无任何装饰框体

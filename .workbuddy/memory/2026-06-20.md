
## 12:35 - 倒计时插件：横幅定位改为动态计算
- **问题**：倒计时横幅使用固定 `left:15%; top:calc(50%+4vw)` 定位，横屏时位置偏移严重
- **方案**：移除 CSS 固定定位，改为 JS `positionBanner()` 动态计算
- **定位逻辑**：查找 `.tick` 元素的第一个 `.tick-segment`（小时卡片），将横幅放在其左下方 12px 处

## 22:20 - 修复 BGM 播放器首次播放报错
- **问题**：`bgmPlayer.js` `playTrack` 方法中引用了未定义的 `onLoadStart`，首次点击播放抛出 `ReferenceError`
- **原因**：`loadstart` 事件监听器被添加但对应的 `onLoadStart` 函数从未定义；`showLoadingState()` 已经在设置 `audio.src` 之前同步调用，`loadstart` 监听器完全冗余
- **修复**：移除 `loadstart` 事件监听器（addEventListener 行）及其在 `onCanPlay`/`onLoadError` 中的两处清理代码
- **文件**：`scripts/bgmPlayer.js`，版本号 `v=20260620v` → `v=20260620w`

## 22:22 - 修复 BGM 播放器一直显示加载中
- **问题**：修复 `onLoadStart` 后，播放器一直显示"⏳ 加载中..."，`current-track-cover` 和 `current-track-info` 都不显示
- **根因**：`canplay` 事件监听器在 `audio.src = ...`（第306行）之后才注册（原329行）。当音频被缓存或加载极快时，`canplay` 在注册前就已触发，导致 `onCanPlay` 永远不会被调用
- **修复**：将事件监听器注册移到 `audio.src = ...` 之前，确保先注册再触发加载
- **版本号**：`v=20260620w` → `v=20260620x`

## 22:26 - 音乐播放器添加专辑封面显示
- **需求**：播放音乐时获取并显示封面图片（Jamendo API 已返回 `track.image`）
- **改动**：
  1. `updateCurrentTrackDisplay()`：读取 `currentTrack.image`，设置 `.vinyl-record` 的 `backgroundImage`，添加 `has-cover` 类
  2. `showLoadingState()`：切歌时清除上首封面，避免残留
  3. `renderMusicList()` / `showFavoritesList()`：每行曲目前显示 40×40 缩略图，加载失败时回退到 ♪/❤️ 图标
  4. CSS：`.vinyl-record.has-cover` 深色底 + 图片居中裁剪；`.track-thumb` 圆角缩略图样式
- **文件**：`scripts/bgmPlayer.js`（v→y）、`plugins/bgm-music/style.css`（v→20260620a）、`plugins/bgm-music/plugin.js`

## 22:29 - 修复收藏按钮旁数字不变
- **问题**：点击收藏按钮后，收藏徽章 `favoritesCountBadge` 的数字不变
- **根因**：
  1. `favoriteCurrentTrack()` 只调了 `updateFavoriteButton()`（切换 ❤️ 样式），未更新 `favoritesCountBadge` 的 `textContent`
  2. `updateShowFavoritesButton()` 也只改 `title` 属性，不更新徽章
- **修复**：
  1. `updateShowFavoritesButton()` 增加同步更新 `favoritesCountBadge.textContent`
  2. `favoriteCurrentTrack()` 和 `removeFavorite()` 中增加 `this.updateShowFavoritesButton()` 调用
  3. `plugin.js` 中收藏按钮点击处理增加 `_updateFavCount()` 调用
- **版本**：`v=20260620y` → `v=20260620z`

## 22:30 - 横屏模式音乐播放器上方区域压缩
- **需求**：横屏时 `music-modal-current` 占用太多垂直空间，导致列表需滚动
- **修复**：新增 `@media (max-height: 700px) and (min-width: 500px)` 横屏优化规则
  - 唱片缩小至 70×70，内边距/gap 减半
  - 字体缩小（歌名 18px、艺人 14px）
  - 进度条缩窄至 4px，控制按钮缩小（38px/48px）
  - 音量区、标签页、列表项统一收窄
  - 显式设置 `flex-direction: row` 保持横向布局
- **文件**：`plugins/bgm-music/style.css`（v→20260620c）

## 12:53 - 倒计时横幅：修复横屏下垂直间距过大
- **问题**：横屏模式下横幅离时钟卡片太远（中间隔了名言区域）
- **修复**：
  - 选择器增加 fallback 链（`:scope > .tick-segment` → `:scope > [data-view="group"]` → `.tick-segment:first-child` → `.tick`）
  - 横屏 GAP 从 12px 减至 8px，LEFT_OFFSET 从 4px 减至 2px
  - 横屏底部边界从 60px 放宽至 50px（允许更贴近底部工具栏上方）
- 版本号更新至 `v=20260620c`

## 13:18 - 倒计时横幅：再次修复横屏定位（选择器问题）
- **根本原因**：之前选择器 `:scope > .tick-segment` 无法匹配（中间隔了 `[data-layout]` div），最终回退到整个 `.tick` 容器，导致 `rect.bottom` 是全部3个卡片+名言区域的底部
- **修复**：重写选择器优先级链：
  1. `.tick-flip-panel` — 直接找第一个翻牌面板（最精确，这是用户实际看到的卡片）
  2. `.tick-segment` — 找第一个分段容器
  3. `[data-key="hours"]` — 原始 data-key 属性
  4. `.tick` — 兜底
- **间距进一步缩小**：横屏 GAP=6px, LEFT_OFFSET=0；竖屏 GAP=8px, LEFT_OFFSET=2
- 版本号更新至 `v=20260620d`

## 13:24 - 倒计时横幅：修复横屏下与时钟卡片重叠
- **问题**：GAP=6px 太小，横屏下横幅与第一个时钟卡片（小时"13"）重叠
- **修复**：
  - 横屏 GAP 从 6px → **14px**（时钟卡片更大，需要更多间距）
  - 竖屏 GAP 保持 10px
  - LEFT_OFFSET 统一为 4px（横屏不再为0）
  - 横屏底部边界从 45px 放宽至 55px
- 版本号更新至 `v=20260620e`

## 16:00 - 倒计时横幅：文字颜色智能调整 + 修复空引用错误
- **问题1**：横幅文字颜色硬编码，浅色背景看不清
- **问题2**：前一版只加了 `applyColors` 调用但没有定义方法，导致 `TypeError: this.adjustCountdownBannerColors is not a function`
- **修复**：在 `smartColor.js` 中新增 `adjustCountdownBannerColors()` 方法
  - 深色背景（background brightness < 60）→ 白色文字 + 半透明白色背景 + 白色边框
  - 浅色背景（background brightness ≥ 60）→ 深色文字 + 半透明深色背景 + 深色边框
  - 文字添加 textShadow 增强可读性
- 版本号：`smartColor.js` → `v=20260620a`；`countdown/plugin.js` → `v=20260620f`

## 16:08 - 倒计时横幅：修复智能颜色不生效（时序问题）
- **根因**：`smartColorManager` 初始化时（延迟500ms）横幅 `#countdownBanner` 尚未创建，`adjustCountdownBannerColors` 直接 `return`。之后 banner 创建时不再触发颜色更新
- **修复1**：`smartColor.js` — `applyColors` 中缓存 `_lastTextColor/_lastShadowColor/_lastBorderColor`，`adjustCountdownBannerColors` 无参调用时自动使用缓存值
- **修复2**：`countdown-class.js` — `createBanner()` 中在 `document.body.appendChild(this.banner)` 后主动调用 `window.smartColorManager.adjustCountdownBannerColors()`
- 版本号：`smartColor.js` → `v=20260620b`；`countdown/plugin.js` → `v=20260620g`

## 16:50 - 底部工具栏：支持水平滑动解决按钮溢出
- **问题**：安装多个插件后，底部工具栏按钮超出屏幕宽度无法查看
- **修复**：`styles/controls.css` 将工具栏从固定居中改为水平可滑动
  - 移除 `left:50%; transform:translateX(-50%)` 居中定位 → 改用 `left:0; right:0; max-width:100vw`
  - 新增 `overflow-x: auto` + `-webkit-overflow-scrolling: touch`（iOS 平滑滚动）
  - 隐藏滚动条（`scrollbar-width:none` + `::-webkit-scrollbar{display:none}`）
  - `bottom-tool-btn` 添加 `min-width` + `flex-shrink:0` 防止被压缩
  - 末尾 `::after` 伪元素提供滚动末端呼吸空间
  - 所有响应式断点同步调整（`min-width` + `flex-shrink:0`）
- 版本号：`controls.css` → `v=20260620a`

## 17:40 - 底部工具栏：修复滑动布局后徽章被裁剪
- **问题**：工具栏 `overflow-x: auto` 创建滚动容器，CSS 规范强制 `overflow-y` 从 `visible` 变为 `auto`，导致按钮右上角徽章（故事数、打卡数等）被裁剪
- **修复**：`styles/controls.css`
  - 主 `.bottom-toolbar`：`align-items` center→flex-end；`padding` 0 20px→14px 20px 0 20px（顶部留 14px 给徽章）
  - 小屏断点（max-width:480px）：padding 0 14px→12px 14px 0 14px
  - 横屏断点（landscape+max-height:500px）：padding 0 12px→10px 12px 0 12px
- 版本号：`controls.css` → `v=20260620b`

## 18:21 - 底部工具栏：添加滑动边缘渐变指示器
- **需求**：按钮超出屏幕可左右滑动，但用户不知道还能滑动，需要直观反馈
- **方案**：iOS 风格边缘渐变 + 箭头指示器
  - 左/右两侧各一个 32px 宽半透明渐变条 + CSS 箭头（‹ ›）
  - 仅在对应方向还有未滚动内容时显示（scrollLeft > 4 显示左侧；未到末尾显示右侧）
  - 点击边缘指示器可平滑滚动 0.7 屏
- **实现**：
  - `styles/controls.css` — `.toolbar-edge` 样式（fixed 定位、渐变背景、箭头伪元素、0.3s 淡入淡出）
  - `scripts/app.js` — `initToolbarScrollIndicators()` IIFE：
    - scroll 事件实时更新指示器显隐
    - MutationObserver 监听按钮增减自动重算
    - resize/orientationchange 自适应高度
    - 延迟 1s/3s 初始检测（等插件渲染完成）
- 版本号：`controls.css` → `v=20260620c`；`app.js` → `v=20260621c`

## 18:24 - 滑动指示器：修复与按钮重叠
- **问题**：第一版用全高度渐变条覆盖在按钮上方，箭头和按钮重叠
- **修复**：改为浮动在工具栏上方的小圆形指示器
  - 24px 圆形毛玻璃按钮（深色半透明背景 + 白色箭头 ‹ ›）
  - 位置：fixed 定位在工具栏上方 4px 处，左右各一个
  - 出现动画：从外侧滑入 + 淡入（translateX + opacity）
  - 点击平滑滚动 0.7 屏
- `syncEdgePosition()` 根据 `toolbar.getBoundingClientRect().top` 计算定位
- 版本号：`controls.css` → `v=20260620e`；`app.js` → `v=20260621e`

## 20:20 - 环境音插件改用 Moodist 真实音源
- **背景**：之前用 Web Audio API 合成的环境音（雨声、风声等）听感不自然，用户要求改用 moodist（https://github.com/remvze/moodist）的真实音频
- **Moodist 音源**：84 个环境音 MP3，按分类组织（nature/rain/animals/places/transport/things/noise），CDN 地址 `https://moodist.mvze.net/sounds/...`，CORS `*` 已开启
- **方案**：双模式引擎
  - 真实音频：`fetch` → `decodeAudioData` → `AudioBufferSourceNode`（loop），解码后缓存到 `bufferCache` Map
  - 噪音（白/粉/棕）：保留 Web Audio API 合成（数学生成，效果准确）
- **Promise 去重**：`loadingPromises` Map 防止同一音源被重复 fetch
- **加载状态**：卡片图标旋转动画 + `pointer-events:none` 防止加载中误触
- **错误处理**：加载失败时卡片红色抖动，从 `activeSounds` 自动移除
- **竞态保护**：`start()` 在 `await` 后检查 `_destroyed` 和 `nodes.has(id)`；`_toggleSound` 在 `await` 后检查 `isPlaying` 清理失败项
- **UI**：按 6 个分类（自然/雨声/动物/场所/器物/噪音）分区块展示，共 28 个音源
- **版本**：plugin.js v3.0.0；style.css v=20260620b；manifest.json v3.0.0

## 20:36 - 环境音补全所有 Moodist 音源
- **问题**：之前只选了 28 个音源，用户发现 Singing Bowl 等缺失
- **补全**：重新获取 Moodist 全部 7 个分类的音源定义，补全到 77 个音源：
  - 自然 12（+呼啸风/踏雪/踏落叶/踏碎石/水滴）
  - 雨声 8（+雨打车顶/雨打伞/雨打落叶）
  - 动物 16（+海鸥/狼嚎/狗吠/马蹄/乌鸦/鲸鸣/蜂巢/啄木鸟/鸡鸣/牛叫/羊叫）
  - 场所 16（+机场/教堂/寺庙/建筑工地/水下/酒吧/地铁站/超市/旋转木马/实验室/洗衣房/餐厅）
  - 交通 6（新增分类：火车/车厢内/飞机/潜艇/帆船/划船）
  - 器物 16（+打字机/纸张/颂钵/烘干机/幻灯机/沸水/气泡/调频收音/摩斯电码/洗衣机/黑胶/雨刮器）
  - 噪音 3（白/粉/棕，保留合成）
- **版本**：plugin.js v3.1.0；style.css v=20260620c；manifest.json v3.1.0

## 20:44 - 环境音混音预设功能（保存/加载/删除/多设备同步）
- **需求**：用户希望混音配置可保存为预设，免得每次重新设置，并支持多设备同步
- **数据模型**：`_settings.presets` 数组，每项 `{id, name, sounds:[{id,vol}], masterVolume, createdAt}`
- **UI**：在总音量上方新增"混音预设"区域：
  - "保存当前混音"按钮 → 内联输入框（不用 prompt），默认名称取活跃音源名拼接
  - 预设卡片：显示前4个音源图标 + 名称 + 删除按钮
  - 空状态提示"还没有保存的混音预设"
- **加载预设**：停止所有 → 应用预设的 sounds/volumes/masterVolume → 更新所有滑块和卡片 → 启动音源
- **删除预设**：从 `presets` 数组过滤移除，重新渲染列表
- **同步**：预设存储在 `ambient_settings` localStorage 对象中，复用已有 `syncAdapter.registerSyncKey('ambient_settings')` 机制，reloadFn 增加 `_renderPresets()` 调用
- **打开模态框**：`_openModal()` 中调用 `_renderPresets()` 刷新列表（防云同步后数据不同步）
- **版本**：plugin.js v3.2.0；style.css v=20260620d；manifest.json v3.2.0

## 20:54 - 环境音内置学习专注预设
- **需求**：用户要求预设几个适合学习专注时用的混音
- **新增 `BUILTIN_PRESETS` 常量**：6 个内置预设（不可删除，不需同步）
  1. 深度专注 🧠 — 棕噪音 + 键盘
  2. 图书馆 📚 — 图书馆 + 小雨
  3. 咖啡馆学习 ☕ — 咖啡馆 + 键盘 + 纸张
  4. 雨天阅读 📖 — 雨打窗 + 小雨
  5. 林间冥想 🌲 — 林间风 + 河流
  6. 深夜静心 🌙 — 蟋蟀 + 棕噪音
- **UI 改动**：
  - `_renderPresetCard(preset, isBuiltin)` 统一渲染函数，内置卡片加 `.builtin` class、图标徽章、描述文字、无删除按钮
  - `_getPresetById(id)` 统一查找（先用户预设再内置预设）
  - `_buildModalHTML` 和 `_renderPresets` 分两个子区域："推荐 · 适合学习专注" + "我的预设"
  - `_deletePreset` 拒绝删除内置预设
- **CSS**：`.builtin` 渐变蓝色背景 + `.preset-card-badge` 图标 + `.preset-card-desc` 描述 + `.ambient-preset-sublabel` 子标签
- **版本**：plugin.js v3.3.0；style.css v=20260620e；manifest.json v3.3.0

## 21:02 - "全部停止"按钮移至预设区域头部
- **需求**：用户要求全部停止按钮不要放页面最后，放在保存当前混音按钮前
- **改动**：从底部全宽按钮移到预设区头部，与"保存当前混音"并排放在 `.ambient-preset-actions` 容器中
- **CSS**：新增 `.ambient-stop-all-btn`（小尺寸红色按钮）+ `.ambient-preset-actions`（flex 容器），删除旧的 `.ambient-stop-all` 全宽样式

## 21:15 - 修复页面刷新后环境音不恢复播放
- **问题**：选择预设（如"咖啡馆学习"）后刷新页面，声音丢失不恢复
- **根因**：`onActivate()` 创建引擎但从不尝试恢复 `activeSounds`；浏览器自动播放策略要求首次用户交互后才能播放音频
- **修复方案**：
  - 新增 `_restorePlayback()` — 创建 suspended AudioContext，注册一次性 `pointerdown`/`keydown` 监听器
  - 首次交互时 `_resumeFn` 触发：resume context + 启动所有 activeSounds + 设置 `_resumeTimestamp`
  - `_toggleSound` 增加 300ms 窗口保护：首次点击不 toggle 已在 activeSounds 中的音源（防止 pointerdown 先于 click 启动合成噪音后被 click 误关闭）
  - `_resumeFn` 中 start 完成后检查 activeSounds 是否已变（用户可能点了预设），不在则立即 stop
  - `_updateCardState` 增加 `activeSounds` 成员判断（加载中/恢复中也显示 active）
  - `_toggleSound` 加入 activeSounds 后立即 `_updateCardState`（不等异步加载）
  - 同步回调增加 `_engine.resume()` 调用
  - `onDeactivate`/`onUninstall` 增加 `_cleanupResumeListener()`
  - CSS 新增 `.resume-pending .ambient-badge` 脉冲动画提示用户点击恢复
- **版本**：plugin.js v3.4.0；style.css v=20260620f；manifest.json v3.4.0

## 21:25 - 预设播放状态可视化标识
- **需求**：用户要求当前播放的预设应有明显标识，能看出正在播放哪个
- **实现方案**：
  - 新增 `_isPresetActive(preset)` — 精确匹配 activeSounds 与预设 sounds（Set 比对，顺序无关）
  - 新增 `_findActivePreset()` — 全量扫描内置+用户预设找当前活跃的
  - 新增 `_updatePresetCardStates()` — 轻量级 DOM 操作（toggle class + 动态添加/移除指示器），不重新渲染列表
  - `_renderPresetCard` 渲染时也判断 active，初始 HTML 即包含正确状态
  - 在 `_loadPreset`/`_stopAll`/`_toggleSound`/`_openModal`/同步回调 中均调用更新
- **UI 效果**：
  - `.active` 卡片：绿色高亮边框(#34C759) + 浅绿背景渐变 + 发光阴影
  - 内置预设 active：蓝色边框(#0071e3) + 蓝绿渐变背景
  - 名称文字变色加粗
  - 图标行右侧出现 `●● 播放中` 绿色标签，圆点有脉冲动画
- **CSS**：`.ambient-preset-card.active` / `.builtin.active` / `.preset-playing-indicator` 及动画
- **版本**：plugin.js v3.5.0；style.css v=20260620g；manifest.json v3.5.0

